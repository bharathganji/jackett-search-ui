{
  "extends": ["./tsconfig.node.json", "./tsconfig.app.json"],
  "compilerOptions": {
    "composite": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": [
    "src", // your source directory
    "env.d.ts" // or "src/env.d.ts" if placed inside src/
  ]
}
